# UKUI Fusion Desktop · 桌面环境设计方案

> 风格定位：以 **UKUI（银河麒麟）** 的交互骨架为基底，融合 **KDE Plasma** 的空间层次与动效语言，吸收 **XFCE** 的轻量克制。
> 配套可交互原型：`index.html`（浏览器直接打开，右上角控制台可切换分辨率 / 主题 / 规范标注层，按 `H` 显示尺寸标注）。

---

## 1. 风格方向决策

| 候选 | 采纳 | 理由 |
|---|---|---|
| **UKUI（主基调）** | ✅ 底部任务栏 + 左下角启动器 + 中文优先的信息层级 | 目标用户为国产 Linux 桌面场景，交互习惯与 Windows/UKUI 一致，学习成本最低 |
| KDE Plasma | ✅ 采纳其「高度分级」阴影体系、半透明毛玻璃面板、贴靠(Snap)与 Alt+Tab 切换器 | 现代感与空间层级清晰，动效曲线克制不花哨 |
| XFCE | ✅ 采纳其「轻量」原则：控件装饰少、动画短促、默认不做坞式放大 | 保证低配机器与嵌入式设备流畅运行 |

一句话概括：**UKUI 的骨架 + Plasma 的层次 + XFCE 的克制**。

---

## 2. 设计令牌（Design Tokens）

### 2.1 色彩

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `brand-500` | `#3790FA` | `#4C9DFB` | 主色：焦点、激活、主按钮（UKUI 科技蓝） |
| `brand-600/700` | `#2A7CE8` / `#1F62C4` | `#3B8BF0` / `#2E74D2` | 悬停加深、按下 |
| `brand-50` | `#E8F3FF` | `rgba(76,157,251,.18)` | 选中底色、导航激活态 |
| `surface-0/1` | `#FFFFFF` | `#1E2229` | 窗口、卡片表面 |
| `surface-2` | `#F5F7FA` | `#262B33` | 侧栏、次级面板、输入框底 |
| `surface-3` | `#EDF0F6` | `#2F3540` | 悬停底色 |
| `surface-glass` | `rgba(255,255,255,.86)` | `rgba(30,34,41,.86)` | 任务栏/弹层毛玻璃基底（blur 22–24px + saturate 160%） |
| `border-1 / 2` | `#E4E8F0` / `#D3D9E4` | `#363C46` / `#454C58` | 一级/二级描边 |
| `text-1/2/3` | `#1F2329` `#4E5969` `#86909C` | `#E8EAED` `#AAB1BD` `#7C8492` | 主文/次文/辅助 |
| `success / warning / danger` | `#23B26D` `#FF9A2E` `#EF5350` | 同浅色 | 语义状态 |

对比度：`text-1` 于 `surface-1` ≥ 12:1，`text-2` ≥ 7:1，`text-3` ≥ 4.6:1，全部满足 WCAG AA。

### 2.2 圆角（统一 2px 视觉语言）

| 令牌 | 值 | 用途 |
|---|---|---|
| `r-xs` | 4px | 徽标、时间轴刻度 |
| `r-sm` | 6px | 按钮、输入框、菜单项、窗口控制按钮 |
| `r-md` | 8px | 任务栏按钮、通知卡、桌面图标选中态 |
| `r-lg` | 12px | 菜单/弹层容器、开始菜单、卡片 |
| `win-radius` | **10px** | 窗口（仅四角，最大化时归零） |

### 2.3 阴影（四级高度，Plasma 式分级）

| 级 | 值 | 用途 |
|---|---|---|
| E1 | `0 1px 2px rgba(16,24,40,.06), 0 1px 3px .10` | 卡片、静态面板 |
| E2 | `0 4px 8px -2px .10, 0 2px 4px -2px .06` | **非激活窗口**、悬浮控件 |
| E3 | `0 12px 20px -6px .14, 0 4px 8px -4px .08` | **激活窗口**、弹层 |
| E4 | `0 24px 48px -12px .24, 0 8px 16px -8px .12` | 拖拽中的窗口、启动器、右键菜单 |

### 2.4 动效

| 令牌 | 值 | 场景 |
|---|---|---|
| `t-fast` 120ms | 悬停底色、按钮反馈 | 即指即现，不等待 |
| `t-base` 200ms | 弹层显隐、开关滑块、Toast | `cubic-bezier(.16,1,.3,1)`（ease-out 快进缓出） |
| `t-slow` 320ms | 窗口移动/缩放/最大化/贴靠 | `cubic-bezier(.2,0,0,1)`（标准减速曲线） |
| 最小化动画 | 200ms，`scale(.92) + translateY(60px)` + 淡出 | 指向任务栏方向的收拢感 |

系统级规则：动画只表达「从哪来、到哪去」，不做循环装饰动画；`prefers-reduced-motion` 时全部过渡降为 0ms。

### 2.5 字体与间距

- 字体栈：`Noto Sans CJK SC → Source Han Sans SC → PingFang SC → Microsoft YaHei → system-ui`；等宽场景 `JetBrains Mono / Noto Sans Mono CJK SC`。
- 字号：11（辅助）/ 12.5（正文次级）/ 13–13.5（正文与控件）/ 14（标题）。
- 间距全部为 **4 的倍数**：4 / 8 / 12 / 16 / 24 / 32。

---

## 3. 桌面布局

```
┌──────────────────────────────────────────────┐
│  桌面图标网格（92×92 单元，gap 8，含 48px 图标） │
│  排列：自左向右 → 自上而下；区域下界 = 任务栏顶   │
│                                              │
│         [ 窗口层 z=100+，层叠管理 ]            │
│                                              │
│  Toast 通知：右上角 16px，340px 宽             │
├──────────────────────────────────────────────┤
│ 任务栏 48px：开始|快速启动|任务按钮|托盘|时钟|显桌 │
└──────────────────────────────────────────────┘
```

- **壁纸**：多层 `radial + linear` 渐变（科技蓝主基调），叠加极淡的 115°/65° 斜线纹理增加质感；右键「更换壁纸」在 3 套预置渐变间切换。
- **图标**：单击选中（蓝底 `brand-500` 42% + 白描边），双击打开；悬停上浮 2px + 背景 16% 白；标签白色 + 投影保证任意壁纸下可读，最多 2 行截断。
- **右键菜单**：桌面（新建/排列/刷新/更换壁纸/显示设置/终端）、图标（打开/重命名/删除/属性）、标题栏（还原/最小化/半屏/置顶/关闭）、任务栏（设置/锁定/显示桌面/并排窗口）。菜单项悬停整行 `brand-500` 反白，危险项悬停 `danger`。

## 4. 任务栏与系统托盘

- 高度：**1366 → 42px / 1920 → 48px / 2K 以上 → 56px**；毛玻璃 `blur(22px) saturate(160%)` + 顶部 1px 描边。
- 结构：`开始按钮(20px 网格标)` → 分隔线 → 快速启动（38px 图标钮，悬停上浮 2px）→ 任务按钮区（`flex:0 1 180px`，min 44 / max 220，超出自动挤压）→ 托盘（网络/音量/电量/通知铃铛）→ 时钟（两行：HH:MM + M月D日，tabular-nums 防抖动）→ 显示桌面细条。
- **任务按钮三态**：默认（图标+标题，灰色）/ 激活（`brand-500` 反白 + 底部 2px 白条）/ 最小化（55% 透明度）。单击行为：激活中→最小化；否则→还原并聚焦（符合 UKUI/Windows 习惯）。
- **托盘弹层**：点击对应按钮在任务栏上方 8px 弹出（网络列表 / 音量滑杆 / 电量详情），毛玻璃 + E4 阴影，点击外部或 Esc 关闭。
- **通知中心**：铃铛带未读红点；面板 380px 宽，卡片左侧 3px 蓝条标记未读，支持「全部清除」；Toast 4.2s 自动消退，与中心共享数据。

## 5. 窗口管理

### 5.1 状态机

```
           ┌────────── 还原/拖出 ──────────┐
           ▼                              │
  ┌─────────────┐   双击/Super+↑   ┌───────────┐
  │   Normal    │ ───────────────▶ │ Maximized │
  │ (自由移动)   │ ◀─────────────── │  (全屏区)  │
  └─────────────┘   还原/Super+↓   └───────────┘
     │      ▲                            ▲
     │      └────── 单击任务按钮 ─────────┘│
     ▼                                   │
  ┌─────────────┐    拖拽到左/右边缘      │
  │ Minimized   │    Super+←/→     ┌───────────┐
  │ (收拢动画)   │ ───────────────▶ │ Snap L/R  │──▶ 再拖出即还原跟随光标
  └─────────────┘                  └───────────┘
```

- **层级与焦点**：全局递增 z-index（100 起）；点击任意部位即聚焦（捕获阶段）；激活窗 E3 阴影 + 白色标题文字，失焦窗 E2 + 标题栏降为 `surface-2`、文字 `text-3`。
- **拖拽**：pointer 事件实现，`clamp` 限制窗口至少保留 140px 在可视区内、顶部不越界；**从最大化/贴靠状态拖出时按光标比例重新定位**，光标贴合屏幕左右边缘 <8px 触发半屏 Snap、顶部 <4px 触发最大化。
- **缩放**：8 向手柄（边 8px、角 14px 命中区），最小 360×220；拖拽/缩放期间禁用过渡（`transition:none`），松手恢复。
- **双击标题栏**切换最大化；标题栏右键出窗口菜单；置顶窗口带 2px 蓝色描边。
- **显示桌面（Super+D）**：记录当前可见窗口集合整体最小化，再按原样恢复。

### 5.2 Alt+Tab

按住 Alt 显示居中切换器（毛玻璃卡片行，按最近使用排序），Tab/Shift+Tab 循环，选中项上浮 4px + 反白；松开 Alt 完成切换。

## 6. 交互细节

| 场景 | 反馈 |
|---|---|
| 悬停 | 120ms 底色渐变；任务栏图标上浮 1–2px；关闭钮悬停变红反白 |
| 按压 | `transform: translateY(-1px)` 回落 + 阴影收紧 |
| 焦点 | `:focus-visible` 2px `brand-500` 外环（纯键盘操作才显示） |
| 弹层 | 200ms `scale(.97)+translateY(6px) → 原位` 淡入 |
| 窗口打开 | 级联偏移 26px/窗，避免完全重叠 |
| Toast | 右上滑入 24px，4.2s 自动退出 |

### 快捷键表

| 快捷键 | 功能 | | 快捷键 | 功能 |
|---|---|---|---|---|
| `Super` | 启动器 | | `Super+↑/↓` | 最大化 / 最小化·还原 |
| `Alt+Tab` (+Shift) | 切换窗口 | | `Alt+F4` | 关闭窗口 |
| `Super+D` | 显示桌面 | | `Ctrl+Alt+T` | 终端 |
| `Super+←/→` | 左/右半屏 | | `Esc` / `F5` | 关闭弹层 / 刷新桌面 |

## 7. 分辨率与窗口自适应策略

| 档位 | 逻辑宽度 | 尺度调整 |
|---|---|---|
| 1366×768 | 紧凑 | 任务栏 42px、图标 88px、弹层 `min(620px,46vw)` 自收缩 |
| 1600–1920 | 标准 | 48px / 92px |
| 2560×1440 | 放大 | 56px / 104px（等效 1.25×） |
| 3840×2160 | 放大 | 同上档（等效 2.0×，HiDPI 由合成器处理） |

- 原型内置分辨率模拟器，窗口位置尺寸在切换后自动重新约束到可视区。
- **窗口内自适应**采用 CSS Container Query：`<560px` 侧栏折叠为 56px 图标条，`<380px` 隐藏侧栏；任务按钮 `flex-basis:180px` 可压缩至 44px 仅显示图标——保证任意窗口尺寸下布局不破。
- 所有弹层使用 `min(px, vw/vh)` 双约束，小屏不溢出。

## 8. 无障碍（WCAG AA）

- 全部交互元素可 Tab 聚焦，菜单/弹层支持 Esc 关闭；`aria-label` 标注窗口控制按钮。
- 对比度 ≥ 4.5:1（正文）、3:1（大字与图形）；焦点环与悬停态使用双色冗余（底色 + 描边）。
- 动画尊重 `prefers-reduced-motion`；触控目标 ≥ 36px（托盘）/ 44px（主按钮）。

## 9. Qt / QML 落地映射（工程侧）

| Web 原型 | Qt 实现 |
|---|---|
| 设计令牌 `:root{--…}` | QML 单例 `Theme.qml`（property real/color）或 QSS 变量 + `qApp->setPalette()`；深浅色用 `QQuickStyle` + 自定义 palette 切换 |
| 毛玻璃任务栏 | Qt 6：`MultiEffect`（blur+saturation）；Qt 5.12：`ShaderEffectSource` + 自定义 blur shader，或 X11 下读取根窗口像素做背景模糊（ukui-kwin 已有 blur 接口） |
| 窗口动画（t-slow 320ms） | QML `Behavior on x/y/width/height { NumberAnimation { easing.type: OutCubic; duration: 320 } }`；Widgets 用 `QPropertyAnimation` |
| 8 向缩放/拖拽 | 交由窗口管理器（ukui-kwin / KWin）处理，应用侧仅需响应 `QWindow::startSystemMove()/startSystemResize()` |
| Alt+Tab 切换器 | KWin Effect（QML scripting）或独立全屏 QML 窗口（`Qt.WindowStaysOnTopHint` + 屏蔽输入转发） |
| 托盘/通知 | `QSystemTrayIcon` + `org.freedesktop.Notifications`（notify-send 协议）；通知中心读 History 接口 |
| GSettings 配置（ballsize 同款做法） | `org.ukui.panel.style` schema 存 `panel-height / theme / taskbar-centered`，QML 端用 `QQmlGSettings`（ukui 项目现有封装） |
| 圆角窗口 | Wayland：`uuid` 协议 / X11：`QRegion` mask 或 KWin blur+rounded-corners 装饰 |
| Container Query 侧栏折叠 | QML `Loader` + `width` 绑定切换布局，或 `Layout` 优先级（`Layout.minimumWidth` + 组件状态机） |

## 10. 交付物清单

| 文件 | 说明 |
|---|---|
| `index.html` | 单文件可交互原型：完整窗口管理、任务栏、启动器、通知、日历、右键菜单、快捷键、分辨率模拟、深浅主题、规范标注层 |
| `DESIGN-SYSTEM.md` | 本文档：令牌、规范、状态机、快捷键、Qt/QML 映射 |

## 11. v2 修订：开始菜单重构与性能分级

> 背景：系统后续运行在低性能设备（飞腾 E2000 系列 / RK 系列，CPU 约为当代 x86 单核 1/6；E2000 无 3D GPU，RK 出厂常落 llvmpipe 软渲染），设计之初即按"最坏情况 = 无 GPU 软件光栅化"设底线。

### 11.1 开始菜单：紧凑单页（Whisker 演进）

```
┌────────────────────────────┐
│ 搜索框（打开即聚焦，防抖 160ms，结果截断 20 条，   │ 34px
│         支持中文 / 拼音 / 首字母）                 │
├──────┬─────────────────────┤
│ 分类 │ 应用单列列表          │
│ 76px │  行高 40px           │
│ 全部 │  图标 24 + 名称 13px  │
│ 常用 │  悬停整行变色         │
│ 办公 │  ↑↓ 键盘选择 + Enter │
│ 开发 │                     │
│ 影音 │                     │
│ 系统 │                     │
├──────┴─────────────────────┤
│ 头像 kylin        锁屏   电源   │ 46px
└────────────────────────────┘
固定 344×486（1366 档 320 / 2K 档 400），不支持 resize
```

- **为何放弃原网格启动器**：图标网格单元 96px + 文字换行的填充与排版成本约是单列列表的 3 倍，且逐项 hover 上浮动画在软渲染下每帧重绘整屏。
- **常用分类**：按启动频次动态排序（WM 计数），首次使用给默认 6 项 —— 对齐 ukui-menu 的收藏心智，免配置。
- **搜索即一切**：对齐 Win11 / Kickoff / ukui-menu 的共同趋势 —— 打开菜单输入框即聚焦，直接输入即可，分类仅作兜底浏览。
- **元素预算**：整屏约 40 个可视节点（原网格方案约 120+），单列行高固定，滚动时增量绘制。

### 11.2 性能分级（Perf Tier）—— 三档 + 自动检测

| 档 | 背景 | 阴影 | 动效 | 触发条件 |
|---|---|---|---|---|
| **完整 full** | 毛玻璃 blur(22px)+saturate | 四级阴影 E1–E4 | 320ms 几何动画 + 位移/缩放 | 仅手动选择（高配设备） |
| **均衡 balanced** | 静态半透明（无实时模糊） | 静态阴影 | 200ms，无逐项动画 | 检测到硬件加速（自动默认） |
| **极简 minimal** | 纯色不透明 | 全部关闭，1px 描边替代 | 仅 120ms 淡入，0ms 几何动画，无任何位移/缩放 | 检测到 llvmpipe/SwiftShader/无 WebGL（自动） |

- **自动检测**：读取 `WEBGL_debug_renderer_info` 的 UNMASKED_RENDERER，命中 `llvmpipe|swiftshader|softpipe|software` 即判软渲染 → 极简；无 WebGL（无头/嵌入式）同样保守降级极简。检测失败不阻断启动。
- **手动覆盖**：设置内保留档位开关，允许用户在低配设备上强制完整档（自担风险）。
- **落 Qt 时的等价物**：检测 `QSGRendererInterface::GraphicsApi` / `glGetString(GL_RENDERER)`；三档映射到 QML 单例 `Perf.qml` 的属性开关，毛玻璃仅在 full 档挂 `MultiEffect`，minimal 档窗口动画用 `QQuickWindow` 直写不挂 Behavior。

### 11.3 软渲染渲染成本红线（设计评审 checklist）

| 禁止（每帧重算） | 允许（一次计算可缓存） |
|---|---|
| 实时背景模糊 / backdrop-filter | 静态预渲染模糊贴图 |
| 逐项 stagger 入场动画 | 单次 opacity + 位移 |
| ≥3 层半透明叠加（overdraw） | 圆角 + 1px 描边 |
| 大半径动态阴影随滚动重算 | 静态阴影绘制一次 |
| 无虚拟化的长列表滚动 | 列表虚拟化 / 分页 / 行高固定 |
| 滚动中实时光栅化 SVG 图标 | 图标预光栅化位图缓存（限定 sourceSize） |
| 输入每字符重建列表 | 防抖 160ms + 结果截断 |

---

## 12. v3 修订：低分辨率兼容（最低 800×600）

### 12.1 密度档位（html[data-size]）

| 档 | 视口宽 | 任务栏 | 图标格 | 开始菜单 | 窗口最小 | 基准字号 |
|---|---|---|---|---|---|---|
| **xs** | ≤900（含 800×600） | 38px | 74px | 292px | 300×180 | 13px |
| **sm** | ≤1280 | 42px | 86px | 320px | 340×200 | 14px |
| **md** | ≤1920（默认） | 48px | 92px | 344px | 360×220 | 14px |
| **lg** | ≥2560 | 56px | 104px | 400px | 360×220 | 14px |

所有尺度差异由 `html[data-size]` 的 CSS 变量统一驱动，禁止在 JS 里往内联样式打补丁 —— 保证档位切换是原子的、可回退的。

### 12.2 任务栏降级阶梯（保证「全部可点」）

设计原则：**先压缩，再隐藏文字，最后折叠进溢出菜单；任何被折叠的元素都必须在溢出菜单里能找到，不允许直接裁掉。**

1. **压缩**：间距 6→3px、按钮 38→32/30px；
2. **隐藏次要文字**：开始按钮只留图标、时钟只留时间、托盘按钮去文字；
3. **折叠快速启动**：四个快速启动图标收进任务栏 `»` 溢出按钮（点击弹出菜单，仍可启动）；
4. **任务按钮溢出**（运行时测量，`fitTaskbar()`）：
   - 任务区放不下 → 先整体切「图标-only」模式（42px 方块）；
   - 仍放不下 → **最早的窗口按钮**逐个进入溢出菜单，计数显示；**当前焦点窗口的按钮永不折叠**；
   - 溢出菜单列出全部窗口（含已最小化标注），点击即聚焦/最小化。

### 12.3 低分辨率可达性检查清单（验收标准）

- [x] 800×600 下开始菜单完整可见（292×396，分类栏可滚动）
- [x] 任务栏所有按钮、托盘、时钟、显示桌面均 ≥24px 可点击区域
- [x] 窗口最小 300×180，缩放/拖拽不会把标题栏按钮推出屏幕
- [x] 弹层（右键菜单/托盘/日历）定位做 viewport 边界 clamp
- [x] 窗口内容 ≤380px 时侧栏转为顶部横向滚动条，**不隐藏导航**
- [x] 设置搜索框在 ≤520px 容器宽度时隐藏（可跳过，不承载关键功能）

---

## 13. v3 修订：设置中心 —— 模块化 DEB 架构

### 13.1 设计立场

设置主程序（`ukui-control-center`）**只是一个壳**：负责窗口、侧栏、搜索与模块调度。功能全部下沉为独立 DEB 包（`ukui-control-<id>`），装几个显示几个，卸掉即刻消失，模块之间零编译期依赖。

### 13.2 包规约（Linux 落地约定）

| 项 | 约定 |
|---|---|
| 包名 | `ukui-control-<id>_<ver>_<arch>.deb` |
| 可执行 | `/usr/bin/ukui-control-<id>`（独立进程入口） |
| 桌面入口 | `/usr/share/applications/ukui-control-<id>.desktop`（`NoDisplay=true` 或可见均可） |
| 模块注册 | `/usr/share/ukui-control-center/modules/<id>.desktop`（壳扫描此目录生成侧栏） |
| 配置存储 | GSettings `org.ukui.control-center.<id>` |
| 内置页 | 「模块管理」「关于本机」随主包安装，不可卸载 |

### 13.3 三种运行形态

1. **设置内打开**：壳扫描 modules 目录 → 侧栏生成 N 项 → 点击惰性构建页面；
2. **单独运行**：`ukui-control-power` 直接启动，只有该模块的窗口（顶部显示包名+版本），不加载壳与其他模块；
3. **开机菜单 / 启动器**：每个已安装模块在启动器「设置」分类中出现，可固定到任务栏或桌面。

### 13.4 性能设计（打开速度第一）

| 手段 | 说明 |
|---|---|
| **首帧同步构建** | 打开设置只构建「侧栏 + 头部 + 当前一个模块页」，其余模块点击时才构建 |
| **页面节点缓存** | 构建过的页面 DOM 节点保留复用（`PAGE_NODES`），切回 = 重新挂载，0 重建 |
| **行级更新** | 开关/选项片/滑块只改自身 DOM 与状态表（`MOD_ST`），不重建页面 |
| **搜索防抖 200ms + 截断 12 条** | 避免每字符全量过滤 |
| **设置窗口禁用毛玻璃** | 即使 full 档也不挂 backdrop-filter —— 设置是高频低延迟场景 |
| **耗时自证** | 头部徽章实时显示「冷启动 x ms / 首帧构建 x ms / 缓存复用 x ms / 搜索过滤 x ms」 |

Qt 落地等价物：页面用 `Loader` + `asynchronous:false`（首模块）/ `true`（其余），缓存用 `Component` 复用；模块发现 = `QDirIterator("/usr/share/ukui-control-center/modules", {"*.desktop"})`；单独运行 = 每模块一个 `QCoreApplication` 入口或 `QCommandLineParser` 子命令。

### 13.5 原型中的 10 个模块

`pkgmgr 模块管理(内置) · power 电源 · display 显示 · sound 声音 · wallpaper 壁纸与个性化 · network 网络 · account 用户账户 · datetime 时间与日期 · update 系统更新 · about 关于本机(内置)`

控制台「设置模块（DEB 装卸模拟）」可实时装卸：侧栏条目、启动器「设置」分类、独立窗口三处联动；卸载时该模块的独立窗口自动关闭。

---
*UI Designer · 2026-09-22（v3） · Ready for developer handoff*
